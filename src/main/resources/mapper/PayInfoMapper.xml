<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dflong.storecontract.mapper.PayInfoMapper">

    <resultMap id="BaseResultMap" type="com.dflong.storecontract.entity.PayInfo">
        <id column="pay_no" property="payNo" jdbcType="VARCHAR"/>
        <result column="contract_id" property="contractId" jdbcType="VARCHAR"/>
        <result column="contract_item_id" property="contractItemId" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="total_amount" property="totalAmount" jdbcType="DECIMAL"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="create_by" property="createBy" jdbcType="VARCHAR"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="update_by" property="updateBy" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        pay_no, contract_id, contract_item_id, status, total_amount,
        create_time, create_by, update_time, update_by
    </sql>

    <select id="selectByPayNo" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List"/>
        FROM pay_info
        WHERE pay_no = #{payNo}
    </select>

    <select id="selectByContractId" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List"/>
        FROM pay_info
        WHERE contract_id = #{contractId}
        ORDER BY create_time DESC
    </select>

    <select id="selectByContractItemId" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List"/>
        FROM pay_info
        WHERE contract_item_id = #{contractItemId}
    </select>

    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List"/>
        FROM pay_info
        WHERE status = #{status}
        ORDER BY create_time DESC
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List"/>
        FROM pay_info
        ORDER BY create_time DESC
    </select>

    <insert id="insert" parameterType="com.dflong.storecontract.entity.PayInfo">
        INSERT INTO pay_info (
            pay_no, contract_id, contract_item_id, status, total_amount,
            create_time, create_by, update_time, update_by
        ) VALUES (
            #{payNo}, #{contractId}, #{contractItemId}, #{status}, #{totalAmount},
            #{createTime}, #{createBy}, #{updateTime}, #{updateBy}
        )
    </insert>

    <update id="update" parameterType="com.dflong.storecontract.entity.PayInfo">
        UPDATE pay_info
        SET
            contract_id = #{contractId},
            contract_item_id = #{contractItemId},
            status = #{status},
            total_amount = #{totalAmount},
            update_time = #{updateTime},
            update_by = #{updateBy}
        WHERE pay_no = #{payNo}
    </update>

    <delete id="deleteByPayNo">
        DELETE FROM pay_info
        WHERE pay_no = #{payNo}
    </delete>

    <update id="updateStatus">
        UPDATE pay_info
        SET status = #{status}, update_time = NOW()
        WHERE pay_no = #{payNo}
    </update>

</mapper>