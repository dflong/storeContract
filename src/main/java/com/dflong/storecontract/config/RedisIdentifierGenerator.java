package com.dflong.storecontract.config;

// Placeholder removed implementation to avoid interfering with MyBatis-Plus IdentifierGenerator resolution.
// ID generation is handled by RedisIdMetaObjectHandler which fills @TableId fields on insert.
public class RedisIdentifierGenerator {
    // intentionally left blank
}
